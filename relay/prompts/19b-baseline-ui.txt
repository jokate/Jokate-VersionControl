19b단계: baseline 모델(19a)을 웹 UI·에디터 메뉴에 반영 + 타임라인 자동 갱신. 전제: 19a 에서 Store.status() 는 baseline(사용자가 마지막으로 올린 상태) 대비이고, Store.upload(message, only), Store.revert_to_baseline(rels), snapshots.uploaded 가 있다. (1) web.py: GET /api/status 는 그대로 {diff}(이제 baseline 기준), POST /api/snap {message, only} 는 upload 로 연결(응답 형태 유지: snapshot|null, diff=올린 항목, stored), POST /api/revert {assets:[rel], discard_dirty} 신설 → revert_to_baseline 적용(409 규칙은 /api/restore 와 동일), GET /api/revert?asset=... 드라이런. api_log / api_snap 은 uploaded 가 있는 스냅샷을 '올린 항목' 기준으로 직렬화하고 항목에 uploaded:true 표시. api_info 에 pending(올리지 않은 변경 수)과 last_label 추가. (2) index.html: '현재 변경사항' 패널 제목 옆에 기준 표시('마지막으로 올린 #N 이후'), 우클릭 '선택한 것 되돌리기' 는 /api/revert 를 쓰도록(문구: '마지막으로 올린 상태로 되돌리기'), 확인 모달·실행 취소·dirty 처리 흐름은 기존 것 재사용. 타임라인에서 올린 스냅샷(label+uploaded)은 '올림' 필과 올린 개수를, 자동 스냅샷은 지금처럼 흐리게. 타임라인 헤더에 '자동 저장 숨기기' 토글(localStorage 기억) — 켜면 label 만 보인다. (3) 자동 갱신: 5초마다 /api/info 를 폴링해 snapshots 수나 last.id 가 바뀌면 타임라인을 다시 그리고(현재 선택·스크롤 위치·검색어·다중 선택 유지), pending 이 바뀌면 변경사항 패널도 갱신. 탭이 숨겨져 있으면(document.hidden) 폴링 중지. 새 스냅샷이 들어오면 타임라인 맨 위 항목에 잠깐 accent 테두리(transform/box-shadow 만, opacity 0 시작 애니메이션 금지). (4) 에디터: jokate/ue/jokate_client.py 에 revert_assets(base, rels, discard_dirty) 와 revert_preview 추가, jokate_bridge.py 의 콘텐츠 브라우저 메뉴 '되돌리기' 를 '마지막으로 올린 상태로 되돌리기'(revert) 로 바꾸고 확인창 문구 갱신, '선택한 애셋 올리기' 는 그대로 upload 로 동작. (4b) 에디터 실행 판정을 프로젝트별로: 지금 jokate/store.py 의 editor_running() 은 tasklist 에 UnrealEditor.exe 가 하나라도 있으면 참이라, 다른 프로젝트의 에디터가 켜져 있어도 이 프로젝트의 롤백이 '에디터가 켜져 있는데 브릿지가 없다' 로 거부된다(실제로 겪음). editor_running(cfg=None) 으로 바꿔 cfg 가 주어지면 UnrealEditor.exe 프로세스들의 명령줄에 이 프로젝트의 .uproject 경로(대소문자·슬래시 방향 무시) 또는 프로젝트 폴더 경로가 들어 있는 것만 센다. 명령줄 조회는 PowerShell 'Get-CimInstance Win32_Process -Filter ...' 를 subprocess 로(타임아웃 10초, 실패하면 기존 tasklist 방식으로 폴백해 보수적으로 참). 조회 부분은 주입 가능한 함수로 분리해 테스트. apply_restore·uediff·bridge-status 등 호출부는 cfg 를 넘기게 하고, 기존 테스트의 monkeypatch(editor_running 을 람다로 바꾸는 방식)가 인자 유무와 상관없이 계속 동작하도록 호출 시그니처를 맞춘다(람다가 *args, **kwargs 를 받도록 테스트도 정리). (5) tests: api_status 가 자동 스냅샷 이후에도 pending 을 보여줌, /api/snap only → 올린 항목만, api_revert 적용·409, api_log 의 uploaded 직렬화, api_info pending, jokate_client 새 함수 왕복. tests/test_web_static.py 가 있으면 새 요소 확인 추가. README(평소 흐름·되돌리기)·docs/API.md 갱신. 공통 규칙: 기존 디자인 토큰 재사용, 기존 기능(다중 선택, 검색, 리세이브 접기, 묶기, 정리, 내용 변경 표, UE diff, 레이아웃 조정이 들어와 있으면 그것도) 유지. 검증: uv run --no-project --with pytest pytest -q 전부 통과 + python -m py_compile jokate/ue/jokate_bridge.py. 경로는 슬래시만 사용(백슬래시 금지). 실제 에디터 실행 금지, C:/Users/kkkk4017/Projects/MNYS 에서 어떤 jokate 명령도 실행 금지. 편집은 Edit 도구로 파일별로, 일괄 치환 스크립트 금지.
