20b단계: DataAsset 등 일반 애셋의 '속성 값' diff. 배경: 14a 에서 DataTable 은 스냅샷 순간 에디터 브릿지가 내용을 JSON 으로 뽑아 sha 키 사이드카(.jokate/store/meta/<sha>.json)에 저장하고 두 버전을 표로 비교한다. 같은 방식을 일반 애셋으로 넓힌다. 실제 UE 5.7.4 에디터에서 확인한 사실: unreal.AssetExportTask + unreal.ObjectExporterT3D + unreal.Exporter.run_asset_export_task 로 커스텀 DataAsset(YSAbilityDataAsset)을 T3D 텍스트로 내보낼 수 있고, 결과는 'Begin Object Class=... Name="ABILDATA_Test" ...' 다음에 '   BasicAttackAbilities(0)=(AbilityClass="...")' 같은 속성 줄들이다(task.object=asset, task.filename=임시 경로, automated=True, prompt=False, replace_identical=True, exporter=ObjectExporterT3D()). (1) jokate/ue/jokate_bridge.py 의 op export_meta 확장: DataTable 은 지금처럼 kind 'DataTable', 그 외 클래스가 TEXT_META_CLASSES 규칙에 맞으면 T3D 로 <project>/Saved/JokateMeta/ 아래 임시 파일에 내보내 읽은 뒤 파일을 지우고 {kind:'Text', cls, text} 로 응답. 너무 큰 결과(기본 512KB 초과)는 건너뛰고 errors 에 사유. dirty 패키지는 skipped. (2) jokate/meta.py: META_CLASSES 를 규칙으로 일반화 — 기본 대상: DataTable(표), 그리고 Text 방식은 클래스 이름이 DataAsset 으로 끝나거나 config.toml 의 [meta] text_classes 목록(기본 ['*DataAsset', 'CurveFloat', 'CurveTable', 'InputAction', 'InputMappingContext', 'YS*'])에 fnmatch 로 걸리는 것. Blueprint·머티리얼·텍스처·메시·애니메이션·레벨은 기본 제외(크고 의미 없는 줄이 많음) — [meta] text_classes 에 직접 넣으면 허용. 텍스트 정규화 normalize_t3d(text): 줄 끝 공백 제거, 내보낼 때마다 바뀌는 잡음(예: ExportPath 의 임시 이름, 'Begin Object'/'End Object' 줄은 유지, 포인터·GUID 처럼 매번 달라지는 값이 보이면 정규식으로 마스킹하되 과하게 지우지 말 것) → 순수 함수. diff_text(a, b) → difflib 기반으로 {lines:[{op:' '|'+'|'-', text, a_no, b_no}], added, removed} 문맥 3줄, 너무 길면 자름. 속성 단위 요약 summarize_props(a, b) → 'Name(idx)=' 앞부분을 키로 묶어 {changed:[{key, old, new}], added:[{key,new}], removed:[{key,old}]} (같은 키의 한 줄 변경은 changed 로). (3) web.py api_metadiff 가 kind 'Text' 도 지원: {available, kind:'Text', props:{...}, diff:{...}}. config.py 에 [meta] text_classes 읽기와 DEFAULT_CONFIG 주석. (4) index.html '내용 변경' 섹션: kind Text 면 위에 속성 요약 표(키 | 이전값 → 새값, 추가 초록/삭제 빨강), 아래에 접을 수 있는 원문 diff(+/- 줄 색, 고정폭 글꼴, 줄 번호). 긴 값은 말줄임 + 클릭 시 전체. DataTable 표는 그대로. (5) gc 의 고아 meta 정리는 kind 와 무관하게 동작해야 한다. (6) tests/test_meta.py 추가: normalize_t3d, diff_text, summarize_props(값 변경·항목 추가·삭제·배열 인덱스), 규칙 매칭(기본·config 덮어쓰기·Blueprint 제외), capture_meta 가 Text 응답을 저장, api_metadiff Text. jokate_bridge.py 는 python -m py_compile 로 문법 확인. README '더 깊게 보기'·설정, docs 갱신. 공통 규칙: 기존 디자인 토큰 재사용, opacity 0 에서 시작하는 CSS 애니메이션 금지, 기존 기능 유지. 검증: uv run --no-project --with pytest pytest -q 전부 통과 + python -m py_compile jokate/ue/jokate_bridge.py. 경로는 슬래시만 사용(백슬래시 금지). 실제 에디터 실행 금지, C:/Users/kkkk4017/Projects/MNYS 에서 어떤 jokate 명령도 실행 금지. 편집은 Edit 도구로 파일별로, 일괄 치환 스크립트 금지.
