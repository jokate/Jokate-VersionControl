18b단계: 에디터가 이미 켜져 있으면 'UE 에서 diff 열기' 는 절대 두 번째 에디터를 띄우지 않는다. 현재(18단계 결과): jokate/uediff.py 의 open_diff 는 에디터가 켜져 있고 브릿지가 살아 있으면 브릿지 op 'diff' 로 그 에디터 안에서 diff 창을 열고, 브릿지가 없거나 op 가 실패하면 새 UnrealEditor 프로세스를 -diff 로 띄우는 경로로 폴백한다. 바꿀 동작: (1) store.editor_running() 이 참이면 어떤 경우에도 새 에디터 프로세스를 띄우지 않는다. 브릿지가 죽어 있으면 DiffBlocked(RuntimeError) '에디터는 켜져 있는데 브릿지가 꺼져 있습니다 — 에디터 상단 툴 메뉴의 Jokate > 브릿지 켜기 를 누르거나 에디터를 다시 시작하세요', 브릿지 op 가 실패하면 DiffBlocked 에 브릿지가 돌려준 error 를 담는다(시도한 전략 이름 포함). (2) 새 프로세스로 여는 경로는 에디터가 꺼져 있을 때만. (3) web.py: DiffBlocked 는 409 {ok:false, error, mode:'editor'} 로 매핑(error_response). index.html: 409 면 토스트가 아니라 모달로 안내문을 보여준다. 에디터가 꺼져 있어 새 프로세스로 여는 경우에는 먼저 확인 모달('에디터가 꺼져 있습니다. diff 를 보려면 에디터를 새로 띄워야 하며 1분쯤 걸립니다. 띄울까요?')을 거친 뒤 요청하도록 /api/uediff 에 dry_run 또는 GET /api/uediff/plan?rel=&a=&b= 로 mode 를 미리 알려주는 방법을 추가. (4) CLI uediff 도 같은 규칙, 막히면 exit code 2 와 안내문. (5) 테스트 갱신: 에디터 실행 중 + 브릿지 없음 → DiffBlocked(프로세스 launcher 가 호출되지 않음을 단언), 에디터 실행 중 + 브릿지 op 실패 → DiffBlocked, 에디터 꺼짐 → process 모드, api_uediff 409 본문, plan 엔드포인트. README·docs/API.md·docs/ARCHITECTURE.md 반영. 공통 규칙: 기존 디자인 토큰 재사용, opacity 0 에서 시작하는 CSS 애니메이션 금지, 기존 기능 유지. 검증: uv run --no-project --with pytest pytest -q 전부 통과 + python -m py_compile jokate/ue/jokate_bridge.py. 경로는 슬래시만 사용(백슬래시 금지). 실제 에디터·UnrealEditor 실행 금지, C:/Users/kkkk4017/Projects/MNYS 에서 어떤 jokate 명령도 실행 금지. 편집은 Edit 도구로 파일별로, 일괄 치환 스크립트 금지.
